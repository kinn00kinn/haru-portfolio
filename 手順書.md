# 🚀 `haru-portfolio` プロジェクト管理ガイド

このプロジェクトの全体像、開発の進め方、公開方法、そして守るべき重要なルールを要約します。

### I. 概要 (このプロジェクトは何か？)

* **目的**: Reactで構築されたポートフォリオサイトです。
* **サイトの種類**: **静的サイト**
    * 訪問者ごとに内容が変わらない、完成品のHTML/CSS/JSファイルを見せるシンプルなサイトです。
* **公開場所 (ホスティング)**: **GitHub Pages**
    * GitHubの機能を利用してWebサイトを公開します。
* **公開URL (予定)**: `https://kinn00kinn.github.io/haru-portfolio/`

---

### II. サイト更新の全体像 (どう動いているか？)

このプロジェクトは、開発から公開までが以下の流れで自動化されています。

1.  **開発**: 開発者が `src/App.tsx` などのReactコードを編集します。
2.  **合流**: 変更内容を `main` ブランチに合流（マージ）させます。
3.  **自動デプロイ (GitHub Actions)**: `main` ブランチの更新をきっかけに、GitHubが以下の処理を自動で実行します。
    * (a) 必要なツールをインストール (`npm install`)
    * (b) Reactコードを「静的サイト」用ファイルに**ビルド** (`npm run build`)
    * (c) 完成したファイル (`dist` フォルダ) をGitHub Pagesにアップロード
4.  **公開完了**: 数分後、公開URLに変更が反映されます。

---

### III. 💻 開発の進め方 (自分のPCでの作業)

サイトを修正したり、新しい内容を追加したりする時の手順です。

* **1. 開発環境の準備 (初回のみ)**
    * プロジェクトに必要なツール（ライブラリ）をインストールします。
    * `npm install`
* **2. 開発サーバーの起動 (作業中)**
    * PC上でテスト用のサイトを起動し、変更をリアルタイムで確認できます。
    * `npm run dev`
* **3. (任意) コードの文法チェック**
    * `npm run lint`

---

### IV. 🔄 サイト公開の手順 (Gitの使い方)

安全にサイトを更新するためのルールと手順です。

* **最重要ルール**: **`main` ブランチは直接編集しません。**

* **公開までの流れ:**
    1.  **分岐 (Branch)**: `main` ブランチから、作業用のコピー（例: `feature/update-profile`）を作ります。
    2.  **開発 (Commit & Push)**: 作業用ブランチで III. の開発作業を行い、変更を保存（コミット）し、GitHubにアップロード（プッシュ）します。
    3.  **依頼 (Pull Request)**: GitHub上で「作業ブランチの変更を `main` に合流させてください」という依頼（プルリクエスト）を作成します。
    4.  **合流 (Merge)**: プルリクエストを承認し、`main` ブランチに変更を合流させます。
    5.  **完了**: あとは II. の「自動デプロイ」が実行されるのを待つだけです。

---

### V. ⚠️ プロジェクトの重要ルール (必須設定)

このサイトをGitHub Pagesで正しく動作させるための**2つの必須設定**です。

* **ルール1: `HashRouter` を使用する**
    * **理由**: 静的サイトでは `/about` のようなURLに直接アクセスすると404エラーになるため。
    * **設定**: `src/main.tsx` で `<App />` を `<HashRouter>` で囲みます。
    * **影響**: URLが `.../#/about` のように `#`（ハッシュ）付きになります。

* **ルール2: `base` を設定する**
    * **理由**: サイトがドメイン直下ではなく `/haru-portfolio/` というサブフォルダに公開されるため。
    * **設定**: `vite.config.ts` に `base: '/haru-portfolio/'` と記述します。
    * **影響**: CSSやJSファイルへのパスが正しくなり、デザイン崩れなどを防ぎます。